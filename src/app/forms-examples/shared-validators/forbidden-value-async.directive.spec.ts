import { ForbiddenValueAsyncDirective } from './forbidden-value-async.directive';

describe('ForbiddenValueAsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenValueAsyncDirective();
    expect(directive).toBeTruthy();
  });
});
